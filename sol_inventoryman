#include <cstdlib>
#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>

using namespace std;

bool found = false;
void process_string(string s, string s2)
{
	char freq_table[256] = {0};
	char s2_freq[256] = {0};
	int count = 0, index =-1;
	for(int i=0; i<s.size(); ++i)
	{
		if(s[i] != s2[i]){
			++count;
			index = i;
		}
		if(count > 2)
			return;
	}
	if(count == 1)
	{
		for(int i=0; i <s.size(); ++i)
		{
			if(i != index)
				cout << s[i];
		}
	}
}

int main(void)
{
	ifstream file("input", fstream::in);
	string id;
	vector<string> input;
	while(file>>id)
	{
		input.push_back(id);
	}
	for(int i=0; i<input.size(); ++i)
	{
		for(int j=i+1; j <input.size(); ++j)
		{
			process_string(input[i], input[j]);
			if(found)
				return 0;
		}
	}
	return 0;
}